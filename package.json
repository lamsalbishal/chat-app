{
  "name": "chatappp",
  "version": "1.0.0",
  "description": "Server is for chat",
  "main": "server.js",
  "scripts": {
    "start":"node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
  },

  "engines": {
   "node": "9.10.1"
  },

  "author": "Bishal lamsal",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
